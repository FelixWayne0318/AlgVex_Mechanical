# Prometheus scrape config for AlgVex
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'algvex'
    static_configs:
      - targets: ['localhost:9090']
    metrics_path: '/metrics'
