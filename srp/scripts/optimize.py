#!/usr/bin/env python3
"""
SRP v6.1 Parameter Optimization — Optuna

Usage:
    python3 srp/scripts/optimize.py --days 456 --capital 1500 --trials 200

Optimizes: srp_pct, dca_spacing, mintp, max_loss, rsi_mfi_below, rsi_mfi_above
"""

# TODO: Port from scripts/optimize_srp_params.py
# Uses srp.scripts.backtest.run_backtest() as the objective function.

print("TODO: Port from scripts/optimize_srp_params.py — uses srp/scripts/backtest.py")
